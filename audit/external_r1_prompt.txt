You are an external smart-contract security auditor performing R1 review of a Sui Move AMM package called "Darbitex". The submission bundle (self-audit + inlined sources) was provided as input on stdin.

DELIVERABLE: A markdown audit report with these sections in this order:

1. **Verdict** — one of: GREEN (deployable as-is) / YELLOW (deployable after fixes) / RED (not deployable). One sentence justification.

2. **Findings table** — one row per finding, columns: ID | Severity (HIGH/MEDIUM/LOW/INFO) | Title | Location (file:line or function name). Empty if zero findings.

3. **Per-finding detail** — for each finding:
   - **Severity & Title** (repeat from table)
   - **Location** — file + line range or function
   - **Description** — what is wrong
   - **Impact** — concrete attacker scenario or correctness consequence
   - **Recommendation** — minimal code change to fix

4. **Answers to bundle questions Q1-Q8** — section per question, terse, concrete.

5. **Other observations** — anything else the developer should know but not severity-tagged.

GROUND RULES:
- Read the inlined sources carefully; the self-audit is a starting hypothesis, not a conclusion.
- IGNORE refutations R1-R7 (Position-NFT vs Coin-LP, no arb module, no multisig, no Display, no V3 CLMM, no admin/pause, on-chain WARNING) — these are deliberate design choices, not findings.
- Sui Move dialect: addresses use `package::module`. `Coin<T>` has no callback (no reentrancy from coin operations). `public(package)` restricts cross-package calls. Hot-potato = struct with no abilities, must be consumed in same TX.
- Be specific: "line 234" beats "in flash logic". Numerical edge cases beat hand-waving.
- If you cannot find any issue at a given severity, say so. Don't invent findings to fill rows.
- The pool uses x*y=k V2 math, 5 bps swap fee + 5 bps flash fee, MasterChef-style per-share fee accumulator with per-position debt snapshot, distinct typed FlashReceiptA<A,B> / FlashReceiptB<A,B>, hot-wallet deploy + Tx2 destroy_cap sealing, no admin post-seal.
- Focus first on: flash safety, k invariant, accumulator math, sealing irrevocability, optimal-pair add_liquidity edge cases, cast safety on u256→u64.

Begin the audit now.
